# Foundation Module

This module exists only to keep the AWS IaC foundation format-checkable and statically valid before managed AWS resources are introduced.

It defines common inputs, non-sensitive tag conventions, and metadata outputs. It does not declare AWS providers, EKS, RDS, ECR, ALB, IAM, networking, DNS, or any other managed AWS resource.
