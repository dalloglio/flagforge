apiVersion: v1
kind: Service
metadata:
  name: {{ include "flagforge-api.fullname" . }}
  labels:
    {{- include "flagforge-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "flagforge-api.selectorLabels" . | nindent 4 }}
